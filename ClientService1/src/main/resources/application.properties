
# The name of the service as it will appear in Eureka
spring.application.name=my-client-service

# The URL of the Eureka Server to register with
eureka.client.serviceUrl.defaultZone=http://localhost:8761/eureka/

# Optional but recommended: uses IP address instead of hostname for registration
eureka.instance.preferIpAddress=true
